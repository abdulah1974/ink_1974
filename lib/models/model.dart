class model {
  final String name;
  final String id;
  final String us;
  final String user;
  final String img;
  model(
      {
        required this.name,
        required this.id,
        required this.us,
        required this.user,
        required this.img,
      });
// price: data["price"] == null ? 0 : double.parse(data["price"]),

}

